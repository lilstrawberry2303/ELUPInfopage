Please completely refactor our authentication engine and global state provider (`store.tsx` or `ElupContext.tsx`) to migrate from our custom plain-text username lookup system to official Firebase Authentication, while preserving our existing user management logic.

Please follow these exact technical requirements to complete the migration:

1. AUTHENTICATION SCHEME (THE VIRTUAL EMAIL TRICK)
- Our users login using plain usernames (no real emails). To integrate with Firebase Auth, seamlessly append a virtual domain string under the hood whenever talking to the Auth SDK: `${username.toLowerCase().trim()}@elup.local`.
- The user interface login form must remain unchanged (only prompting for Username and Password fields).

2. DATABASE ARCHITECTURE EVOLUTION
- Shift the user record schema path from `/users/{username}` to `/users/{uid}`, where the Document ID is now the secure Unique ID generated by Firebase Authentication.
- Inside each user document, map these exact fields:
  - `uid`: (string)
  - `username`: (string, lowercase)
  - `role`: (string, e.g., 'manager', 'surveyor', 'technician')
- Change the live user synchronization array (`accounts`) to listen to this new `/users/{uid}` path structure.

3. REFACTOR THE REPLIT CONTEXT MUTATION METHODS
Update our exposed context actions to use the Firebase Auth SDK alongside Firestore:
- `loginWithUsername(username, password)`: Uses `signInWithEmailAndPassword` with the virtual email layout. On success, cache the secure `user.uid` string into browser `localStorage` to handle persistent sessions across page refreshes.
- `onboardUser(account)`: Project Managers must still be able to create accounts. Use `createUserWithEmailAndPassword` using the virtual email convention to register them in Auth, grab the returned `uid`, and write the profile metadata document (`uid`, `username`, `role`) to Firestore.
- `updateUserCredentials(uid, newUsername, newPassword)`: Allow users or managers to update accounts. Update the `username` field directly in the Firestore profile document. If a new password is provided, invoke the Auth SDK's `updatePassword` method on the targeted user session instance.
- `removeUser(uid)`: Delete the user metadata profile from Firestore.

4. COMPONENT STABILITY
Keep all other core data nodes (`blocks`, `recentActivity`, `role`, `activeBlockId`, `chartView`) completely intact so that our dashboards, interactive coordinate grids, and forms do not break or require widespread refactoring.